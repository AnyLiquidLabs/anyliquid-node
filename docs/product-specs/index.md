# Product Specs Index

This directory is reserved for user-facing product specifications, API guarantees, and externally committed behavior.

The current repository snapshot contains design documents but no separate product specification set yet. When those specs are added, index them here instead of placing them at the repository root.
